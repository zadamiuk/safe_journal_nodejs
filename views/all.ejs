
<div class="row mt-4 mb-5">

    <div class="col-md-10 m-auto">
  <div class="card-body">
    <form action="/notes/all" method="GET">
      <table class="table  table-striped">
        <thead>
          <tr>
          <th><h4>Your notes:</h4></th>
          <th><a href="/notes/new" class="btn btn-success">Add new note</a></th>
        </tr>
          </thead>
        <tbody>
        <%Note.forEach( function( data){ %> 
          
          <tr>
            <td><b><%= data.title %></b><br> <%= data.date.toDateString() %></td>
            <td style="width: 20%"><a href="/notes/note/<%= data._id %>" class="btn btn-secondary" class="fixed" data-q_id=data._id > Read (not working)</a></td>
            <td style="width: 20%"><a href="/notes/delete/<%= data._id %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete note <%= data.title %>?');" class="fixed" data-q_id=data._id >Delete</a></td>
         
          </tr>
          <tr>
            <td><p><i><%= data.description %></i></p></td>
          </tr>
        
        <% }); %>
        </tbody>
        </table>
    </form>

  
</form>
<div style ="position: absolute; bottom: 0; left: 85%">
  <a href="/users/logout" class="btn btn-secondary pull-right" >Logout</a>
</div>



